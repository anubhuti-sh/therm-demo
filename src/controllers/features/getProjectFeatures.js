const Org = require('../../models/org');

const getProjectFeatures = async () => {
    const classNameFilter = await Org.findOne({  });
    const temperatureFilter = 
};

module.exports = {
  getProjectFeatures,
};
